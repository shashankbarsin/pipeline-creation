kind: Deployment
metadata:
  name: {{Image.Name}}
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{Image.Name}}
    spec:
      containers:
      - name: {{Image.Name}}
        image: {{ContainerRegistry.Name}}/{{Image.Name}}
        ports:
        - containerPort: {{Container.Port}}
        